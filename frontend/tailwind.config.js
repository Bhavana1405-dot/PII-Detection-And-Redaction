/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,ts,jsx,tsx}", // Scan all your source files
    ],
    theme: {
      extend: {}, // Add custom theme config here if needed
    },
    plugins: [],
  };
  